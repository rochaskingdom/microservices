spring:
  application:
    name: worker

  datasource:
    url: jdbc:h2:file:./worker/target/classes/data/worker_db;AUTO_SERVER=true
    driverClassName: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console